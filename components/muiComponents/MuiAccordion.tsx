"use client";
import Accordion, { AccordionProps } from "@mui/material/Accordion";
import React from "react";

function MuiAccordion(props: AccordionProps) {
  return <Accordion {...props} />;
}

export default MuiAccordion;
